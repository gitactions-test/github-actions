name: tag-filter-flow
on:
    push:
        tags:
            - 'v[0-9]+.[0-9]+.[0-9]+' #v.1.1.1 or v.1.9.9 같은 태그일 때에만 실행 / v1.0 , 1.0.0 은 실행 x

jobs:
    tag-filter:
        runs-on: ubuntu-latest
        steps:
            - name: step 1
              run: echo "tag filter"