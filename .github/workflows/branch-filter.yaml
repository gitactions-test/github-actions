name: filter-workflow
on:
    push:
        branches: ["dev"]

jobs:
    branch-filter:
        runs-on: ubuntu-latest
        steps:
            - name: step1
              run: echo filter test
    