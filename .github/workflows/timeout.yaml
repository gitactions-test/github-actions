name: timeout
on: push

jobs:
    inf-loop:
        runs-on: ubuntu-latest
        timeout-minutes: 3
        steps:
            - name: loop
              run: |
               count=0
               while true; do
                echo "seconds: $count"
                count=$((count + 1))
                sleep 1
               done
            - name: done
              run: echo done