name: always-flow
on: push

jobs:
    job1:
        runs-on: ubuntu-latest
        steps:
            - name: exit1
              run: exit 1
            - name: job1 step 2
              if: always()
              run: echo "job1 step 2"
    job2:
        runs-on: ubuntu-latest
        needs: [job1]
        if: always()
        steps:
            - name: job2 step 1
              run: echo "job2 step 1"
