name: path-filter-workflow
on:
    push:
        paths:
        - '.github/workflows/part1/*'

jobs:
    path-filter:
        runs-on: ubuntu-latest
        steps:
            - name: step 1
              run: echo path filter