name: push-workflow 1
on: push

# 실행 시킬 job의 집합
jobs:
    push-job:
        # job을 실행시킬 러너를 지정 하는 키워드
        runs-on: ubuntu-latest
        #step의 집합
        steps:
        - name: step1
          run: echo hello world
        - name : step2
          run: |
            echo hello world
            echo github action test
    my-job :
        runs-on : ubuntu-latest
        steps :
        - name : step1
          run : echo my world
        - name : step2
          run : echo omg
